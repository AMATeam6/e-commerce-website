# Git Checkout (Old vs New)

## Old command
git checkout

Used for:
- switching branches
- restoring files
- creating branches

Example:
git checkout main

## New commands

### git switch
Used only for switching branches

Example:
git switch main

### git restore
Used to restore files

Example:
git restore file.txt